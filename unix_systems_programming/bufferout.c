#include <stdio.h>

int main(void) {
	fprintf(stdout,"a");
	fprintf(stdout,"a has been written\n");
	fprintf(stdout,"b");
	fprintf(stdout,"b has been written\n");
	fprintf(stdout,"\n");

	return 0;
}
